{
    "hooks": {
        "PostToolUse": [
            {
                "matcher": "^(Edit|MultiEdit|Write)$",
                "hooks": [
                    {
                        "type": "command",
                        "command": "jq -r 'select(.tool_name | test(\"^(Edit|MultiEdit|Write)$\")) | .tool_input.file_path | select(test(\"\\\\.(ex|exs|heex)$\"))' | xargs -I {} mix format 2>/dev/null"
                    }
                ]
            }
        ]
    }
}
